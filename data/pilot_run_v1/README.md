# Pilot Run v1 — Real Executed Data

Unlike everything else in `data/` (which is generated by running `scripts/`
against a fully networked environment this project's main pipeline assumes),
the files in this directory are the actual output of a real, smaller-scope
pilot run executed in a sandboxed session that had no network access to
PubChem, ChEMBL, UCSC, or RCSB PDB directly, but did have:

- pip access to PyPI (installed real `vina`, `openmm`, `openbabel-wheel`,
  `biopython`, `rdkit`, `pypdb`);
- read access to the RCSB PDB's AWS Open Data mirror (`s3://pdbsnapshots`,
  reachable over HTTPS even though `rcsb.org`/`files.rcsb.org` were not);
- apt access, so a full LaTeX toolchain could be installed.

This produced `manuscript/submission/paper.pdf` — a real, self-consistent,
much smaller-scope pilot study (single-target ATR docking with a redocking
validation control), not the full multi-target pipeline the rest of this
repository is designed for. See that paper for the full account.

## Files

- `seed_compounds.csv` — 5 real seed compounds. Three (`berzosertib_VE822`,
  `camonsertib_RP3500`, `CHK1_inhibitor_YM8`) were extracted directly from
  the co-crystallized active-site ligand of PDB 9L40/9L4B/2YM8
  respectively (see `ligands/`); two (`prexasertib`, `ceralasertib`) were
  obtained via web search and cross-verified against independently
  reported molecular formula/exact mass.
- `derivative_library.csv` — 786 BRICS-recombination products passing the
  Lipinski/Veber/PAINS/Brenk pre-filter (from a 5,000-candidate capped
  enumeration over 22 fragments derived from the 5 seeds).
- `developability_predictions.csv` / `developability_passed_compounds.csv`
  — descriptor-rule developability predictions for all 791 seed+derivative
  compounds; 680 passed (≥3/4 rules, no structural alert).
- `docking_candidate_set.csv` — the 40-compound pilot set actually docked
  (5 seeds + top 35 developability-passed derivatives by rule count then
  molecular weight).
- `redocking_validation_results.json` / `redocking_replicate_results.json`
  — the redocking validation control: 9L4B and 2YM8 passed (≤2.0 Å RMSD to
  native pose); 9L40 reproducibly failed (2.96±0.06 Å across 3 seeds)
  despite tight search reproducibility, and was excluded from candidate
  scoring per the paper's pre-specified rule.
- `atr_docking_results_raw.csv` / `atr_docking_results_summary.csv` — the
  real Vina docking results (40 compounds × 3 seeded replicates against
  the validated 9L4B receptor).
- `box_centers.json` — docking box centers (active-site ligand centroids)
  for all three receptor structures.
- `ligands/*.sdf` — the three ligands extracted directly from their
  crystal structures, as parsed and bond-order-perceived by RDKit/Open
  Babel (used as a sanity check against known chemistry — e.g. the 9L40
  extraction resolves to berzosertib's known pharmacophore).
- `build_real_library.py`, `run_redocking_validation.py`,
  `run_redocking_replicates.py`, `run_library_docking.py`,
  `finalize_results.py` — the exact scripts run to produce the above,
  runnable as-is from a directory containing the corresponding receptor
  PDBQT files (regenerate those from the PDB IDs above via
  `scripts/01_fragile_site_mapping.py`-style structure prep, or re-fetch
  from the AWS mirror noted above).

Receptor PDBQT/PDB/mmCIF files themselves are not included here (large,
regenerable, and already excluded from version control by `.gitignore`);
re-download from `s3://pdbsnapshots` (snapshot `20260101`) as documented in
`manuscript/submission/paper.tex` §Methods.
